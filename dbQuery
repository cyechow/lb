-- SELECT * FROM items;
-- SELECT * FROM orders;
-- SELECT * FROM designs;

-- DROP TABLE items;
-- DROP TABLE orders;
-- DROP TABLE designs;

-- CREATE TABLE items(id SERIAL PRIMARY KEY, itemid SERIAL UNIQUE, item VARCHAR(40) not null, count INTEGER);
-- CREATE TABLE designs(id SERIAL PRIMARY KEY, designid SERIAL UNIQUE, designname VARCHAR(40) NOT NULL UNIQUE, designtype INTEGER, description VARCHAR(256) );
-- CREATE TABLE orders(id SERIAL PRIMARY KEY, orderId SERIAL UNIQUE, clientName VARCHAR(40) NOT NULL, orderStatus VARCHAR(256) NOT NULL);