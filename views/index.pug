extends layout.pug

//- Declaration (invoice-mixin.pug)
mixin invoice(number, email)
  .invoice-wrapper
    h2 Invoice
      ul
        li #{number}
        li #{email}

block content
  .container
    p= message
    +invoice('1234567890','test@test.ca')